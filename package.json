{
  "name": "doppler-custom-domain",
  "private": true,
  "devDependencies": {
    "eclint": "^2.8.1",
    "prettier": "^2.5.0"
  },
  "scripts": {
    "verify": "yarn prettier-check && yarn eclint-check",
    "eclint-fix": "eclint fix",
    "eclint-check": "eclint check",
    "prettier-fix": "prettier --write \"**/**\"",
    "prettier-check": "prettier --check \"**/**\""
  }
}
